#
is.loaded=true

# configures logging level for one's tests
log.level=INFO
# configures test log output destination: [CONSOLE | FILE]
log.output=CONSOLE

# test DB - the one your persistence tests will work against..
jdbc.driver=org.h2.Driver
jdbc.url=jdbc:h2:mem:HELLO_DB;INIT=CREATE SCHEMA IF NOT EXISTS HELLO;MODE=Oracle;MV_STORE=FALSE;MVCC=FALSE;
## use this URL when you want to debug
#jdbc.url=jdbc:h2:./target/db/HELLO_DB;INIT=CREATE SCHEMA IF NOT EXISTS HELLO;MODE=Oracle;MV_STORE=FALSE;MVCC=FALSE;AUTO_SERVER=TRUE;
jdbc.username=sa
jdbc.password=
jdbc.schema=HELLO